<header>
  <h1 class="text-center">Rotten Mangoes</h1>
</header>


<!-- <%= render 'search' %> -->

<%= link_to "Submit a movie!", new_movie_path, class: "btn btn-warning btn-lg center-block" %>

<!-- <% @movies.each do |movie| %>
  <%= link_to image_tag(movie.poster_image_url.thumb.url), movie_path(movie) %>
  <h2><%= link_to movie.title, movie_path(movie) %> | <%= movie.review_average %>/10</h2>
  <h3><%= formatted_date(movie.release_date) %></h3>
  <h4>Dir. <%= movie.director %> | <%= movie.runtime_in_minutes %> minutes</h4>
  <p><%= movie.description %></p>
  <hr>
<% end %> -->

<table class="table">
  <%= render @movies %>
</table>